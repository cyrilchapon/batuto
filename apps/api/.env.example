# Local dev defaults. In deployed environments these come from Doppler, never
# set by hand — see .agents/context/engineering/overview/infra-and-envs.md.
DATABASE_URL="postgresql://batuto:batuto@localhost:5432/batuto_dev?schema=public"
PORT=3001
WEB_URL="http://localhost:5173"

# Clerk (backend). Pulled from the Clerk dashboard's own API Keys page, not
# generated locally — see the Batutô Clerk application once provisioned.
CLERK_SECRET_KEY="sk_test_..."
CLERK_PUBLISHABLE_KEY="pk_test_..."

# AppSignal. Pulled from the AppSignal dashboard's push API key, not
# generated locally.
APPSIGNAL_PUSH_API_KEY="00000000-0000-0000-0000-000000000000"

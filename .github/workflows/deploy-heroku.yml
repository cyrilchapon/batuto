name: Deploy API to Heroku

# Imperative, CI-driven deploy — not Heroku's own git-push auto-deploy or
# `heroku.yml`. Heroku's Deploy tab auto-deploy must stay disabled; this
# workflow is the only thing that ships the API. See infra-and-envs.md's
# "Imperative deploys" section.
on:
  push:
    branches: [dev]

jobs:
  deploy-api:
    name: Deploy API to Heroku (Docker)
    runs-on: ubuntu-latest
    environment: Production
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Log in to Heroku Container Registry
        env:
          HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
        run: echo "$HEROKU_API_KEY" | docker login --username=_ --password-stdin registry.heroku.com

      - name: Build image
        env:
          HEROKU_APP_NAME: ${{ vars.HEROKU_APP_NAME }}
        run: docker build -t "registry.heroku.com/$HEROKU_APP_NAME/web" -f apps/api/Dockerfile .

      - name: Push image
        env:
          HEROKU_APP_NAME: ${{ vars.HEROKU_APP_NAME }}
        run: docker push "registry.heroku.com/$HEROKU_APP_NAME/web"

      - name: Release
        env:
          HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
          HEROKU_APP_NAME: ${{ vars.HEROKU_APP_NAME }}
        run: |
          IMAGE_ID=$(docker inspect "registry.heroku.com/$HEROKU_APP_NAME/web" --format='{{.Id}}')
          curl -fsS -X PATCH "https://api.heroku.com/apps/$HEROKU_APP_NAME/formation" \
            -H "Content-Type: application/json" \
            -H "Accept: application/vnd.heroku+json; version=3.docker-releases" \
            -H "Authorization: Bearer $HEROKU_API_KEY" \
            -d "{\"updates\":[{\"type\":\"web\",\"docker_image\":\"$IMAGE_ID\"}]}"

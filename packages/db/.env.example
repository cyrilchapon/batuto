# Local dev default (matches root docker-compose.yml). In deployed environments
# this comes from Doppler (Neon pooled connection string), never set by hand.
DATABASE_URL="postgresql://batuto:batuto@localhost:5432/batuto_dev?schema=public"
